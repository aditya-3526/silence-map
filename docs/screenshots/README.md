# Screenshots

Capture three images for the README by running the app locally (`python3 -m http.server 8077`):

- `globe.png` — the rotating globe with pulsing region markers
- `panel.png` — the region panel slid in for a selected region
- `mutation.png` — the story mutation tracker modal
